# Web tools

This folder owns direct fetching, search through Ketch, supported video transcript extraction, request limits, and web-specific types.

Search backends belong behind the existing search boundary. They should not leak provider-specific behavior into the agent loop.
