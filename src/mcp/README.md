# MCP

Future home of MCP server configuration, connection lifecycle, capability discovery, and conversion into Snaffle's active capability format.

MCP tools should enter through `capabilities/`, not through special cases in the agent loop.
