# Plugins

Future home of plugin manifests, discovery, enablement, and packaged resources. Keep compatibility adapters here; keep core tool behavior in `tools/`.

The public boundary should emerge from two or three real extensions rather than speculative framework design. Plugins run out of process, declare filesystem, network, credential, process, and UI permissions separately, and contribute data or schema-driven views before arbitrary renderer components are considered.
