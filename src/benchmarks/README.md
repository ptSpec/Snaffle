# Benchmarks

Future home of benchmark definitions, harness runs, and result reporting.

Benchmarks should invoke the same agent, provider, context, and execution boundaries used by the app so results measure the real product behavior.
